<!DOCTYPE html>
<html lang="en">
<head>
    <title>Home</title>
</head>
<body class="bg-dark text-white">
    <%- include('./nav') %>
    <% if(user){ %>
        <% for(let movie of user.movieID){ %>
            <div class="card d-inline-flex bg-secondary" style="width: 18rem;">
                <img src= <%= movie.poster %> class="card-img-top" alt="...">
                <div class="card-body bg-dark text-white" >
                    <h5 class="card-title"><%= movie.name %></h5>
                    <p class="card-text"> <%=movie.year %> &nbsp; <%= movie.time %> &nbsp; ⭐<%= movie.rating %> <%= movie.genre %> </p>
                    </form>
                </div>
            </div>
        <% } %>
    <% }  %>

</body>

</html>
